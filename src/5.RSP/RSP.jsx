const React = require('react');
const { Component } = React;

class RSP extends Component {
    render() {
        return(
            <>
                <h1>가위바위보</h1>
            </>
        )
    }
};

module.exports = RSP;